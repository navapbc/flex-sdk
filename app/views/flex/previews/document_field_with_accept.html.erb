<%= flex_form_with(model: model, url: false) do |f| %>
  <%= f.document_field :state_id_document,
      label: "Supporting Documents",
      hint: "Upload PDF, Word documents, or images only",
      accept: ".pdf,.doc,.docx,.jpg,.jpeg,.png" %>
<% end %>
