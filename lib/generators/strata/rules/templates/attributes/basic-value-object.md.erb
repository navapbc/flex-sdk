---
paths:
  - "**/app/models/**/*.rb"
---

# Strata SDK: BasicValueObjectAttribute

Base class used internally by MoneyAttribute, TaxIdAttribute, USDateAttribute, YearMonthAttribute, and YearQuarterAttribute. Not used directly — consult the specific attribute sub-rule instead.

```ruby
<%= read_sdk_file("app/lib/strata/attributes/basic_value_object_attribute.rb") %>
```
