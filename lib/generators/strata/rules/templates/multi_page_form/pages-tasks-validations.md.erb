---
paths:
  - "**/app/flows/**/*_flow.rb"
  - "**/app/models/**/*_form.rb"
---

# Strata SDK: Multi-Page Forms — Pages, Tasks, Validations

`QuestionPage`, `Task`, and `ApplicationFormValidations` are the three core building blocks that back the flow DSL — they hold page metadata, evaluate task state, and wire per-page validation contexts onto the form model.

## QuestionPage Source

```ruby
<%= read_sdk_file("app/models/strata/flows/question_page.rb") %>
```

## Task Source

```ruby
<%= read_sdk_file("app/models/strata/flows/task.rb") %>
```

## ApplicationFormValidations Source

```ruby
<%= read_sdk_file("app/models/strata/flows/application_form_validations.rb") %>
```

## Validation Contexts

`ApplicationFormValidations#validate_flow(FlowClass)` introspects the flow and auto-generates a constant for each page name (e.g., `Flow::NAME`, `Flow::DATE_OF_BIRTH`). Consumers declare standard Rails validations scoped to these constants using `on:`. In each `update_<page>` action the controller runs `flow_record.valid?` then `flow_record.save(context: page.name)` — scoped validators fire during the save, so only the fields relevant to that page are validated on each step:

```ruby
class LeaveApplicationForm < Strata::ApplicationForm
  include Strata::Flows::ApplicationFormValidations
  validate_flow LeaveApplicationFlow

  validates :applicant_name_first, presence: true, on: Flow::NAME
  validates :date_of_birth_date, presence: true, on: Flow::DATE_OF_BIRTH
end
```

This keeps validations co-located with the model while still running them one page at a time during the multi-step flow.
