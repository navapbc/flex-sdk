<%%%= render template: 'flex/tasks/index', locals: { 
  tasks: @<%= name.underscore.pluralize %>, # Replace @<%= name.underscore.pluralize %> with your array of tasks
  task_types: @task_types,  # Replace @task_types with your array of task types to show in this view
  unassigned_tasks: @unassigned_<%= name.underscore.pluralize %> # Replace @unassigned_<%= name.underscore.pluralize %> with your array of tasks that aren't assigned to any users
} %>
