<%%%= render template: 'flex/tasks/show', locals: {
    task: @<%= name.underscore.singularize %>, # Replace @<%= name.underscore.singularize %> with your Flex task to use
    assigned_user_display_text: @assignee&.name || "Not assigned", # Replace this with the string you want shown for the assigned user, or a string such as "Unassigned", etc.
    application_form: @application_form # Replace @application_form with your Flex application form to use
} %>
