<%= render template: 'strata/tasks/show', locals: {
    task: @task,
    application_form: @application_form,
    task_info: [
      { label: "Status:", value: @task.status.camelize },
      { label: "Due On:", value: local_en_us(@task.due_on) },
      { label: "Assigned To:", value: @assignee&.full_name || strata_link_to("Assign to me", "#", as: :button) }
    ]
} %>
